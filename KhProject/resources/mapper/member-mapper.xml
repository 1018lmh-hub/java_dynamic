<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="memberMapper">
 	<insert id="insertMember"
 			parameterType="member">
 	
 	INSERT
 	  INTO
 	       WEB_MEMBER
 	       (
 	       USER_NO
 	     , USER_ID
 	     , USER_PWD
 	     , USER_NAME
 	     , EMAIL
 	       )
 	VALUES
 	       (
 	       SEQ_MEM_NO.NEXTVAL
 	     , #{userId}
 	     , #{userPwd}
 	     , #{userName}
 	     , #{email}
 	       )
 	
 	</insert>
 
 	<select id="login"
 		    resultType="member"
 		    parameterType="member">
 		SELECT
 			   USER_NO userNo
 			 , USER_ID userId
 			 , USER_NAME userName
 			 , EMAIL
 			 , ENROLL_DATE enrollDate
 			 , MODIFY_DATE modifyDate
 			 , STATUS
 		  FROM
 		       WEB_MEMBER
 		 WHERE
 		       USER_ID = #{userId}
 		   AND
 		       USER_PWD = #{userPwd}
 		   AND
 		       STATUS = 'N'
 		   <!--  별칭 부여말고 settings 도 써보기
 		   
 		   		그러면 탈퇴 후에 같은 ID로 재가입이 안되는 게 UNIQUE 제약 조건 때문인가보네
 		   		그러면 보관기간이랑 진짜 삭제는 안이루어지나
 		   		어차피 내가 고민할 문제는 아니지만
 		   		4:45
 		   		
 		    -->
 	
 	</select>
 
 
 
 
 </mapper>